<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- The native Zennopay SDK requests CAMERA at runtime and degrades to a
       paste-QR fallback on denial. The host app must also declare this. -->
  <uses-permission android:name="android.permission.CAMERA" />
</manifest>
